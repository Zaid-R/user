name: pharmacy_in_pocket
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^2.8.0
  firebase_auth: ^4.3.0
  provider: ^6.0.5
  shared_preferences: ^2.1.0
  firebase_storage: ^11.1.0
  cloud_firestore: ^4.5.0
  flutter_barcode_scanner: ^2.0.0
  collection: ^1.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:

  uses-material-design: true
